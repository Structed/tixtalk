{$TICKETS_HOST:localhost} {
    reverse_proxy pretix:80
}

{$TALKS_HOST:localhost} {
    handle /media/* {
        root * /srv/pretalx
        file_server
    }
    handle {
        reverse_proxy pretalx:80
    }
}
